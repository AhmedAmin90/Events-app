module FriendshipsHelper
    # def add_friend(user)
    #     friend = Friendship.find_by(user_id: current_user.id , friend_id: user.id , status: nil)
    #     if friend
    #       link_to('Reject', friendship_path(id: friend.id, user_id: event.id), method: :delete)
    #     else
    #       link_to('Accept', friendships_path(event_id: event.id), method: :post)
    #     end
    #   end
end
